name: Auto Build APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'

      # Regenerate folder Android dengan memaksakan nama project valid (lowercase & tanpa titik)
      - name: Re-generate Android Project
        run: |
          rm -rf android
          flutter create . --project-name esp32_dashboard --platforms=android --org=com.esp32.dashboard

      - name: Get Dependencies
        run: flutter pub get

      - name: Build Release APK
        run: flutter build apk --release

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Dashboard-ESP32-APK
          path: build/app/outputs/flutter-apk/app-release.apk
